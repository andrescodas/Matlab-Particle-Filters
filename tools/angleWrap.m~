function newAngle = angleWrap(angle)

newAngle = angle;

for i = 1: length(angle)
    
    else
        
end

if(length(angle) > 1 )
    
    
    
else


    while(newAngle > pi)
        newAngle = newAngle - 2*pi;
    end
    while(newAngle <= -pi)
        newAngle = newAngle + 2*pi ;
    end

end